services:
  hello:
    image: hello-world
    deploy:
      replicas: 2

  hello-manager-only:
    image: hello-world
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager

  hello-global:
    image: hello-world
    deploy:
      mode: global
